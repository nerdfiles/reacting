/**
 * @name WPHeader
 * @description
 * footer
 * @param {Object.props}
 */
const WPHeader = ({ props }) => {
  return (
    <>
      <header
        itemScope
        itemType='https://schema.org/WebHeader'
      >
        <h1>header</h1>
      </header>
    </>
  )
}

export default WPHeader

// EOF
